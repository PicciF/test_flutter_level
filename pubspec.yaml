name: test_flutter_level
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.2 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.8.1
  auto_route_generator: ^5.0.3
  path_provider: ^2.1.2
  device_preview: ^1.1.0
  http: ^1.2.1
  auto_size_text: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.3.3
  json_serializable: ^6.6.2
  flutter_secure_storage: ^9.0.0


flutter:
  uses-material-design: true
